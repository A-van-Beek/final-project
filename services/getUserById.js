import { PrismaClient } from "@prisma/client";

const getUserById = async (genre, available) => {
//   const prisma = new PrismaClient();

//   return prisma.book.findMany({
//     where: {
//       genre,
//       available,
//     },
//   });
// };

export default getUserById;
